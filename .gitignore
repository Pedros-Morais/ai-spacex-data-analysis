# Byte-compiled / optimized
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Test / coverage artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.ruff_cache/
.mypy_cache/

# Jupyter
.ipynb_checkpoints/

# Raw API snapshots (regenerated by ingestion); keep the processed fallback CSV.
data/raw/*.json

# NOTE: model artifacts (models/*.joblib, *.json) and figures
# (reports/figures/*.png) ARE committed on purpose: the Streamlit app runs
# without retraining and the README embeds the SHAP/EDA plots. Regenerate any
# time with `make train`.

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
